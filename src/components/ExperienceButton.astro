---
const { title, active } = Astro.props;
---

<button
  id={title.toLowerCase() + 'Button'}
  data-active={active ? 'true' : undefined}
  class="text-sm bg-transparent py-1 rounded-lg cursor-pointer data-active:bg-gray-950"
  >{title}</button
>
