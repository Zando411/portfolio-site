---
const { title, description, imageURL, skills, live, repo } = Astro.props;
import { Image } from 'astro:assets';
const showButtons = live || repo;
---

<div
  class="bg-gray-950 outline-1 outline-gray-700 rounded-lg p-4 shadow-sm flex flex-col h-full"
>
  {
    live ? (
      <a href={live} target="_blank">
        <Image
          src={imageURL}
          alt={title}
          loading="lazy"
          width="800"
          height="600"
          class="rounded mb-2 md:h-52 w-full object-cover object-top aspect-[16/9]"
        />
      </a>
    ) : (
      <Image
        src={imageURL}
        alt={title}
        loading="lazy"
        width="800"
        height="600"
        class="rounded mb-2 md:h-51 w-full object-cover object-top aspect-[16/9]"
      />
    )
  }

  <h2 class="text-2xl leading-5 font-bold font-sans mt-4">{title}</h2>
  <p class="mt-2 text-sm text-gray-400">{description}</p>
  <div class="mt-2">
    <ul class="flex flex-wrap gap-2">
      {
        skills.map((skill) => (
          <li class="text-gray-400 bg-gray-950 outline-1 outline-gray-700 rounded px-2 py-1 text-xs">
            {skill}
          </li>
        ))
      }
    </ul>
  </div>

  {
    showButtons ? (
      <div class="mt-auto pt-4 gap-2 flex items-center">
        {live ? (
          <a
            href={live}
            target="_blank"
            class="text-black flex place-items-center gap-1 bg-white outline-1 outline-gray-700 hover:bg-gray-300 rounded px-2 py-1 text-xs font-bold transition-colors duration-200"
          >
            View Live
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="14"
              height="14"
              viewBox="0 0 24 24"
              fill="none"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="stroke-black w-3.5 h-3.5"
            >
              <circle cx="12" cy="12" r="10" />
              <line x1="2" y1="12" x2="22" y2="12" />
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" />
            </svg>
          </a>
        ) : null}
        {repo ? (
          <a
            href={repo}
            target="_blank"
            class="text-black flex place-items-center gap-1 bg-white outline-1 outline-gray-700 hover:bg-gray-300 rounded px-2 py-1 text-xs font-bold transition-colors duration-200"
          >
            Source
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="stroke-black w-3.5 h-3.5"
            >
              <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22" />
            </svg>
          </a>
        ) : null}
      </div>
    ) : null
  }
</div>
